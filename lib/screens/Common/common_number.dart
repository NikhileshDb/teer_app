import 'package:flutter/material.dart';

class CommonNumber extends StatelessWidget {
  const CommonNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('COMMON NUMBER'),
      ),
    );
  }
}
